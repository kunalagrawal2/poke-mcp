{
  "name": "poke-mcp",
  "version": "1.0.0",
  "description": "Pokémon information MCP server",
  "main": "index.js",
  "type": "module",
  "bin": {
    "poke-mcp": "./build/index.js"
  },
  "scripts": {
    "test": "npm run build && node build/test-client.js",
    "test:client": "npm run build && node build/test-client.js",
    "test:interactive": "npm run build && node build/interactive-test.js",
    "test:cache": "npm run build && node build/cache-test.js",
    "test:offline": "npm run build && node build/offline-test.js",
    "build": "tsc && node -e \"require('fs').chmodSync('build/index.js', '755')\"",
    "start": "node build/index.js",
    "dev": "npm run build && npm start"
  },
  "keywords": [
    "pokemon",
    "pokedex",
    "mcp",
    "model-context-protocol"
  ],
  "author": "",
  "license": "MIT",
  "files": [
    "build"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0",
    "better-sqlite3": "^12.2.0",
    "zod": "3.24.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  }
}
